{
  "buildCommand": "pnpm --filter @workspace/vibes run build",
  "outputDirectory": "artifacts/vibes/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/"
  }
}
